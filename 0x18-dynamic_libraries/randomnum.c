#include <stdio.h>
#include <stdlib.h>

/**
 * jackpot_random_number - a function generates a random
 * number using the rand() function.
 *
 * Return: An integer representing the generated random number.
 */
int jackpot_random_number()
{
	return rand();
}
