#include <stdio.h>

/**
 * main - using the printf function to print strings of characters
 *
 * description: use only the printf function and not puts
 *
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
